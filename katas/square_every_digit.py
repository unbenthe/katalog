def square_digits(num):
    square = ""
    for i in str(num):
        square += str(int(i) ** 2)
    return int(square)

#definir variável para guardar resultados
# por integral na lista:
# a variável se torna o valor de cada ítem da variante integral que existe na lista vezes ele próprio
#retornamos a lista de valores integrais

def count_by(x, n):
    return [i * x for i in range(1, n+1)]
        
    # i integral, range = sequencia de numeros que começa no zero e aumenta de um em um (default) e para em numero especificado.
